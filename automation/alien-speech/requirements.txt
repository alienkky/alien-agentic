fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
python-dotenv>=1.0
requests>=2.31
openai>=1.30
tzdata>=2024.1                          # Windows 는 IANA tz db 미내장 → KST 변환에 필수
# faster-whisper 는 fallback 전용 — STT_MODE=faster-whisper-local 일 때만 import
# pip install faster-whisper

# Tests
pytest>=8.0
httpx>=0.27
