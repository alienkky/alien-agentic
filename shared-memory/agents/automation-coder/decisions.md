# automation-coder — decisions (무엇을 결정했나)

- 2026-06-20 · ALI-115 · edgeId→TopoDS_Edge 역매핑을 **순서 계약**(explorer 재실행)으로 구현. 대안(영구
  edge-handle 레지스트리)은 셰이프 변경 시 동기화 부담 + 메모리 누수 위험 → 기각. 전제: 수집 루프가
  registerAndTessellate 모서리 루프와 동일 순회여야 함(주석으로 못 박음).
- 2026-06-20 · ALI-115 · 필렛/모따기 e2e 는 캔버스 모서리 픽킹이 깨지기 쉬워 신규 추가 안 함 → 단위테스트 +
  디바이스 게이트로 커버. 대신 선행 깨져 있던 기존 스모크를 현행 UI(삽입 플라이아웃)로 갱신.
