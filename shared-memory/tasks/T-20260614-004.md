---
id: T-20260614-004
title: ALIEN SPACE Phase 2 — 스케치 + 돌출
owner: automation-coder
client: _self
priority: P1
due: 2026-07-19
status: in-progress
created: 2026-06-14T07:55
---

# ALIEN SPACE Phase 2 — 스케치 + 돌출

부모: **T-20260614-001** (EPIC) · 선행: T-003(Phase 1, done)

## 목표
지면(XZ) 위에 점을 찍어 다각형 프로파일을 그리고 → 돌출 → 솔리드. (Shapr3D 핵심 흐름)

## 산출물 (슬라이스 1)
- `kernel/geometryToTessellated.ts` — BufferGeometry→TessellatedMesh 공용 유틸(불리언·돌출 공유)
- `kernel/extrude.ts` — THREE.ExtrudeGeometry 로 임의 다각형 +Y 돌출 (검증됨)
- store: sketchActive/sketchPoints + beginSketch/addSketchPoint/undoSketchPoint/cancelSketch/commitExtrude
- `viewport/SketchLayer.tsx` — 지면 탭 레이캐스트로 점 찍기 + 프로파일 프리뷰
- `ui/SketchBar.tsx` + 툴레일 스케치 버튼
- 스케치 중 셰이프 픽킹/기즈모 비활성

## 검증
- tsc strict 0 · vitest 40/40 (extrude 3 신규) · vite build 성공

## 남은 것 (다음 슬라이스)
- 사각형/원 빠른 그리기(드래그), 스냅(격자·끝점)
- 평면 선택(기존 면 위에 스케치) — 지금은 지면 고정
- 돌출 높이 입력(현재 고정 4), 컷 돌출(빼기 돌출)
- 디바이스 검증: 지면 탭 정확도(터치/펜), 프로파일 그리기 손맛

## 진척 로그
- 2026-06-14 07:55 · 슬라이스 1 — 지면 다각형 스케치 → 돌출. 검증 tsc0/vitest40/build.
- 2026-06-14 08:05 · 기즈모 버그 수정(object 직접 제어 — 위치 저장/튕김 해결)
- 2026-06-14 08:55 · **Shapr3D 튜토리얼 로드맵**(`SHAPR3D-ROADMAP.md`) + 순차구현 시작
  · 슬라이스 2 — **스케치 도구**: 사각형(2모서리)·원(중심+반지름)·선(폴리라인) + 격자 스냅(0.5)
  · 도구 선택 바, 시작점 마커. 검증 tsc0 · vitest 43/43 · build
- 2026-06-14 09:15 · **스케치 방식 Shapr3D식 재설계** (기영님: "방식이 너무 다르다")
  · 점 찍기 → **드래그로 그리기**(누르고 끌어 놓기) + 러버밴드 실시간 미리보기
  · **실시간 치수**(W×H / R) Html 오버레이 · 스케치 진입 시 **정면(탑다운) 뷰** 자동
  · 스케치 중 한 손가락 드래그=그리기(궤도 비활성), 두 손가락=줌
  · 검증 tsc0 · vitest 44/44 · build. 남은: 치수 타이핑 입력·끝점 스냅·면 위 스케치
- 2026-06-16 23:55 · **슬라이스 3 — 드래그 후 W×H/⌀ 타이핑 입력** (ALI-111)
  · store `sketchDimEdit` + `commitSketchDim/cancelSketchDim` · 정책(음수·0·NaN 거부, Esc 유지) store 한 자리.
  · `SketchLayer.DimensionEditor` 오버레이 — Tab/Enter 커밋, Esc 취소. 포커스 트랩(`sketchDimEdit` 게이팅)으로 카메라·드래그·선분 편집 차단.
  · 검증 tsc0 · vitest 100/100 (신규 6) · vite build · playwright 슬라이스3 e2e 2/2 통과 (pre-existing smoke 의 "박스 (Box)" 라벨 어긋남은 슬라이스 외).
  · 인사이트: `shared-memory/insights/2026-06-16-alien-space-phase2-typed-dim.md`. 다음: 슬라이스 4 끝점/중점 스냅.
- 2026-06-17 10:30 · **그리드 & 스케치 설정 슬라이스** (ALI-111, Shapr3D 튜토리얼 요청) — 솔버 불필요 절반만 완결
  · 직교/원근 투영 토글(store `projection` + drei OrthographicCamera + CameraRig orthoZoom)
  · 그리드 크기 잠금(`gridLock` + `gridCellSize`), ViewportControls 오배선(snap.grid) 수정
  · 키보드 단축키(`resolveShortcut` 순수함수 + `useKeyboardShortcuts`): 도구 핫키 L/A/I/R/C/G/T, Ctrl+Z/Ctrl+A/Esc
  · 단위 연동(`units.ts`): mm 내부기준, 표시·입력 경계에서만 변환. SketchLayer/Measure/ExtrudeDrag 라벨+입력 전부
  · 검증 tsc0 · vitest 114/114(신규 14) · build · e2e 슬라이스 4/4
  · 인사이트: `shared-memory/insights/2026-06-17-alien-space-grid-sketch-settings.md`
  · **남은 큰 덩어리**: 영상 나머지 절반(자동구속·끝점/중점 스냅·가이드선·박스선택·구성선·구속핫키)은 2D 구속 솔버(PlaneGCS, Module 7) 에픽으로 분리.
- 2026-06-17 10:45 · **그리기-시점 스냅 슬라이스** (ALI-111, "다음 진행") — 구속 에픽의 1단계, 솔버 불필요
  · `kernel/sketchSnap.ts` 순수 모듈: collectSnapPoints/nearestSnapPoint/axisSnap/resolveSnap (우선순위 point>axis>grid)
  · store `sketchSnapHint` + computeSketchSnap → sketchClickPoint/sketchDragMove(점 도구) 적용. snap 토글 게이팅.
  · SketchLayer SnapHintView: 끝점=주황구/중점=청록옥타/축=보라 점선 가이드. snapHint 표시 토글 존중.
  · 검증 tsc0 · vitest 124/124(신규 10) · build · e2e 슬라이스 4/4
  · 인사이트: `shared-memory/insights/2026-06-17-alien-space-sketch-snap.md`
  · **다음**: 진짜 구속 *지속성*(planegcs vs 자체 솔버) — 기영님 결정 대기. 박스선택·구성선·구속핫키도 그 에픽.
- 2026-06-17 14:55 · **네비큐브 (View Controls 챕터)** (ALI-111, "다음 튜토리얼 영상 미구현분") — 영상1 종료자막 "next for view controls" → 다음 챕터
  · `navCubeData.ts` 순수: 6면+8꼭짓점 방향, `cubeTransform` CSS-3D 회전. `cameraMath.viewDirection`(임의 방향 정렬, 면·모서리·꼭짓점 공통) + `spinView`(90° 스핀). store `setViewDir`/`spinView`.
  · `NavCube.tsx`: CSS-3D 큐브 위젯 — 면/꼭짓점 클릭=시점 스냅, 드래그=orbit, 더블클릭=기본뷰, 둘레 4화살표=스핀. 큐브가 카메라 방향 반영.
  · 교훈: 짐벌락 가드는 sinP 아닌 **수평성분 hypot(dx,dz)** (polar 클램프 0.02 때문). CSS-3D 위젯 e2e 는 좌표 히트테스트가 래퍼에 가려 → `dispatchEvent("click")` 직접 디스패치로 배선 검증, 기하는 단위테스트.
  · 검증 tsc0 · vitest 134/134(신규 10) · build · e2e desktop 6/6(navcube 신규 2)
  · 인사이트: `shared-memory/insights/2026-06-17-alien-space-navcube-view-controls.md`
  · **남은(챕터 내)**: 모서리 12 클릭 핸들(데이터만 존재), Space Mouse HW. **여전히 분리**: 2D 구속 솔버 에픽.

- 2026-06-17 21:00 · **모서리 12 마감 + 2D 구속 솔버 슬라이스 1 (자체 자산)** (ALI-111, "후속질문 셋 진행·3번 자체")
  · 네비큐브 `CUBE_EDGES` 12 + NavCube 모서리 핸들 → View Controls 챕터 26 핫스팟 전부 노출(면6+모서리12+꼭짓점8).
  · **`kernel/constraintSolver.ts`** (자체, 외부 0): LM 비선형 최소제곱. 구속 8종(coincident/h/v/distance/fixX/fixY/parallel/perp). 고정점 제외, 수치야코비+자체 가우스소거. 순수.
  · 교훈: **LM 감쇠는 가법** — 곱셈감쇠는 구속 없는 자유변수의 0 대각을 0으로 남겨 특이행렬→무동작. `A_aa = JᵀJ_aa + λ(1+JᵀJ_aa)` 로 수정(테스트 6건이 잡음).
  · 검증 tsc0 · vitest 148/148(신규 14: 솔버11+모서리3) · build · e2e desktop 6/6
  · 인사이트: `shared-memory/insights/2026-06-17-alien-space-constraint-solver-slice1.md`
  · **다음(솔버 슬라이스 2+)**: 스토어 연결(드래그 라이브 재해)·자동구속 추론·구속 UI(핫키/표시/구성선/박스선택).

- 2026-06-17 22:05 · **구속 솔버 슬라이스 2 — 스케치 연결(수평/수직 적용)** (ALI-111, "쉐이퍼 구속 동일하게")
  · 매듭: 스케치=독립 폴리라인(공유정점 없음). Shapr3D 파라메트릭 핵심=공유코너.
  · `kernel/sketchConstraints.ts` 브릿지(순수): `extractVertices`(tol 0.6 정점병합)·`applyToStrokes`(전역인덱스 되쓰기)·`buildSegmentSolve`(세그먼트 H/V+앵커)·`autoSegKind`.
  · store `applySketchConstraint('horizontal'|'vertical'|'auto')`: 선택 세그먼트를 자체 솔버로 재해, 앵커 고정. 공유코너 동반 이동.
  · `SketchConstraintsPanel` "수평/수직"(⇧V) 버튼 실연결, 나머지 정직하게 회색 "준비 중".
  · 정직: **apply-on-demand**(영구 구속·드래그 라이브 재해는 슬라이스 3). 자동구속은 typed-dim 회귀 위험으로 보류.
  · 검증 tsc0 · vitest 155/155(신규 7) · build · e2e desktop 7/7(구속 신규 1)
  · 인사이트: `shared-memory/insights/2026-06-17-alien-space-constraint-solver-slice2.md`
  · **다음(슬라이스 3)**: 영구 구속 스토어 + 드래그 라이브 재해 · 자동구속 추론 · 다중선택(평행/직각/동일/대칭) · 구속 글리프.

- 2026-06-17 23:30 · **자동 구속 (그릴 때 H/V 정렬)** (ALI-111, "2,3번 진행" → Q2 자동구속 선택)
  · 선 도구 폴리라인 확정 시 축 근접(±8°) 세그먼트를 자체 솔버로 정확히 수평/수직 정렬. `prefs.auto` 게이팅(Shapr3D 자동구속 토글).
  · `kernel/sketchConstraints.ts`: `nearAxisKind`(8° 이내만, 대각 null)·`buildAutoConstraints`. store `autoConstrainStroke` — sketchClickPoint/finishLine 의 `tool==='line'` 분기에서 호출.
  · **곡선 파괴 방지**(핵심): 원/호/스플라인도 폴리라인 저장 → 전체 적용 시 곡선 망가짐. 방어 2겹 — (1) line 스트로크만 호출, (2) 다른 스트로크 정점 전부 fixed + 앵커 고정. 새 선 자유 끝점만 정렬.
  · 회귀 안전: typed-dim(rectangle 도구)은 미호출 → 무영향.
  · 환경: **node_modules 사라져 있어 npm install 재실행**(세션 간 워크트리 재체크아웃 추정).
  · 검증 tsc0 · vitest 158/158(신규 3) · build · e2e desktop 8/8(자동구속 신규 1)
  · 인사이트: `shared-memory/insights/2026-06-17-alien-space-auto-constraint.md`
  · **다음**: 다중 선택 관계 구속(평행/직각/일치/동일) · 영구 구속+드래그 라이브 재해(슬라이스 3) · 구속 글리프.

## 회고 (완료 시 채움)
- 잘된 자리:
- 어긋난 자리:
- 다음에 다르게:
