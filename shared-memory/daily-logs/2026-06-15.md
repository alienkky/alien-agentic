---
date: 2026-06-15
weekday: 월요일
---

# 2026-06-15 (월) — Alien Agentic

## 오늘 한 일

### ALIEN SPACE — 1차 배치 (직접 모델링용 한 묶음) + 마우스 데모
기영님 발주: "다음 부분을 전체적으로 알아서 1차로 전부 만들어 + 만든 기능으로 컴퓨터 마우스를 스케치부터 만들어봐"

- **스케치 도구 확장**
  - 타원(ellipse): 중심에서 가로·세로 드래그 → 반장축/반단축
  - 다각형(polygon): 중심에서 드래그 → 정육각형(`POLYGON_SIDES`)
  - 드래그 미리보기·치수 라벨·좌측 팔레트 배선(준비중 → 동작)
- **STL 내보내기**
  - `meshesToStl`: 바이너리 STL 직렬화(이동 오프셋 반영)
  - 파일 메뉴 `STL 내보내기` → 다운로드
- **파이프라인 검증 — 컴퓨터 마우스** (`buildMouse`)
  - 타원 스케치 → 돌출 → 구 교집합(돔) → 슬롯/홈 빼기. 실제 커널 함수만으로 모델링
  - 삽입 플라이아웃 `데모: 마우스`(앱 즉시 로드)
  - 무의존 소프트웨어 렌더러(z버퍼+램버트)로 PNG 미리보기 산출 → 기영님께 PNG+STL 전달

### 교훈 — 손수 짠 UV구는 CSG에서 빈 결과
- `box ∩ 손수짠구` = **0 삼각형**. 원인: UV 스피어의 극점 부채꼴 + 이음새 중복열 → 비매니폴드 → three-bvh-csg 처리 불가.
- 박스∩박스는 정상. **곡면 공구 솔리드는 매니폴드 THREE 지오메트리**(`SphereGeometry`/`CylinderGeometry` → `geometryToTessellated`)로 만들어야 CSG가 동작.
- 또 한 함정: 과한 돔(구가 바닥까지 깎음) → 바닥이 렌즈/톱니로 깨짐. **바닥 전체가 구 안에 들도록** 큰 구·낮은 중심 → 평평한 바닥 + 둥근 윗면 가장자리.

### 검증
- `tsc` strict **0** · `vitest` **54/54** · `vite build` **성공**
- PR #14 갱신(push + 본문). 마우스 STL 633→437 삼각형(3D 프린트 가능 솔리드)

### 2라운드 (기영님: "실제 Shapr3D 기능 구현됐나? 안 됐으면 그 방식 만들어 + 파일 어떻게 불러와?")
- **정직한 평가**: 지금까진 "Shapr3D처럼 보이는 셸 + 기본 파이프라인". **돌출이 고정 4mm(가짜)**, **STL import 없음**이 진짜 모델링을 막던 두 구멍.
- **Shapr3D식 돌출 구현**: 돌출 다이얼로그(높이 + 새 바디/빼기/합치기). `extrudeSketchAs` — cut/fuse 는 AABB 겹친 바디에만 불리언(`kernel/aabb.ts`). 빼기=구멍, 합치기=보스. → 증명 이미지 `extrude-proof.png`
- **STL 불러오기 구현**: `parseStl`(바이너리/ASCII) → 파일메뉴 'STL 불러오기' → addMesh. 받은 .stl 을 앱에 다시 띄움.
- 검증: tsc 0 · vitest **60/60** · build OK. 커밋 2건 푸시(PR #14).

### 불러오기 방법 (기영님 안내)
1. `git pull origin claude/web-3d-modeling-app-1lc7aj`
2. `cd E:\AlienAgentic\alien-agentic\projects\web-3d-modeler\app` → `npm install` → `npm run dev`
3. 브라우저에서 콘솔의 Local 주소 열기
4. 마우스 보기: **삽입 → 데모: 마우스**(파일 불필요) / 또는 받은 STL: **파일 → STL 불러오기**

### 3라운드 (기영님: "다음 진행 계속") — 회전체(Revolve)
- `kernel/revolve.ts`: `revolveProfile` — THREE.LatheGeometry 로 스케치 프로파일을 축(세로 v / 가로 u) 둘레 회전 → 평면 basis 정렬. 닫힌 폴리라인 보장.
- 회전 다이얼로그(각도 1~360, 프리셋 90/180/360 + 축). store `revolveSketchAs`/`openRevolve`. 도구→회전 배선.
- 증명: 폰/꽃병 실루엣 330° 회전 → 깔끔한 축대칭 솔리드 (`revolve-proof.png`).
- 검증 tsc 0 · vitest **63/63** · build OK. PR #14 푸시. SPEC-MAP 4.2 Revolve ✅.

## 내일 첫 작업
- 면 위 스케치(임의 면 선택→그 평면) + 돌출 드래그(면 잡고 실시간 높이) — Shapr3D 직접조작 핵심.
  (sketchPlane: PlaneId → SketchPlaneDef 일반화 필요 — 면 centroid·법선에서 평면 생성)
