---
date: 2026-06-15
weekday: 월요일
---

# 2026-06-15 (월) — Alien Agentic

## 오늘 한 일

### ALIEN SPACE — 1차 배치 (직접 모델링용 한 묶음) + 마우스 데모
기영님 발주: "다음 부분을 전체적으로 알아서 1차로 전부 만들어 + 만든 기능으로 컴퓨터 마우스를 스케치부터 만들어봐"

- **스케치 도구 확장**
  - 타원(ellipse): 중심에서 가로·세로 드래그 → 반장축/반단축
  - 다각형(polygon): 중심에서 드래그 → 정육각형(`POLYGON_SIDES`)
  - 드래그 미리보기·치수 라벨·좌측 팔레트 배선(준비중 → 동작)
- **STL 내보내기**
  - `meshesToStl`: 바이너리 STL 직렬화(이동 오프셋 반영)
  - 파일 메뉴 `STL 내보내기` → 다운로드
- **파이프라인 검증 — 컴퓨터 마우스** (`buildMouse`)
  - 타원 스케치 → 돌출 → 구 교집합(돔) → 슬롯/홈 빼기. 실제 커널 함수만으로 모델링
  - 삽입 플라이아웃 `데모: 마우스`(앱 즉시 로드)
  - 무의존 소프트웨어 렌더러(z버퍼+램버트)로 PNG 미리보기 산출 → 기영님께 PNG+STL 전달

### 교훈 — 손수 짠 UV구는 CSG에서 빈 결과
- `box ∩ 손수짠구` = **0 삼각형**. 원인: UV 스피어의 극점 부채꼴 + 이음새 중복열 → 비매니폴드 → three-bvh-csg 처리 불가.
- 박스∩박스는 정상. **곡면 공구 솔리드는 매니폴드 THREE 지오메트리**(`SphereGeometry`/`CylinderGeometry` → `geometryToTessellated`)로 만들어야 CSG가 동작.
- 또 한 함정: 과한 돔(구가 바닥까지 깎음) → 바닥이 렌즈/톱니로 깨짐. **바닥 전체가 구 안에 들도록** 큰 구·낮은 중심 → 평평한 바닥 + 둥근 윗면 가장자리.

### 검증
- `tsc` strict **0** · `vitest` **54/54** · `vite build` **성공**
- PR #14 갱신(push + 본문). 마우스 STL 633→437 삼각형(3D 프린트 가능 솔리드)

### 2라운드 (기영님: "실제 Shapr3D 기능 구현됐나? 안 됐으면 그 방식 만들어 + 파일 어떻게 불러와?")
- **정직한 평가**: 지금까진 "Shapr3D처럼 보이는 셸 + 기본 파이프라인". **돌출이 고정 4mm(가짜)**, **STL import 없음**이 진짜 모델링을 막던 두 구멍.
- **Shapr3D식 돌출 구현**: 돌출 다이얼로그(높이 + 새 바디/빼기/합치기). `extrudeSketchAs` — cut/fuse 는 AABB 겹친 바디에만 불리언(`kernel/aabb.ts`). 빼기=구멍, 합치기=보스. → 증명 이미지 `extrude-proof.png`
- **STL 불러오기 구현**: `parseStl`(바이너리/ASCII) → 파일메뉴 'STL 불러오기' → addMesh. 받은 .stl 을 앱에 다시 띄움.
- 검증: tsc 0 · vitest **60/60** · build OK. 커밋 2건 푸시(PR #14).

### 불러오기 방법 (기영님 안내)
1. `git pull origin claude/web-3d-modeling-app-1lc7aj`
2. `cd E:\AlienAgentic\alien-agentic\projects\web-3d-modeler\app` → `npm install` → `npm run dev`
3. 브라우저에서 콘솔의 Local 주소 열기
4. 마우스 보기: **삽입 → 데모: 마우스**(파일 불필요) / 또는 받은 STL: **파일 → STL 불러오기**

### 3라운드 (기영님: "다음 진행 계속") — 회전체(Revolve)
- `kernel/revolve.ts`: `revolveProfile` — THREE.LatheGeometry 로 스케치 프로파일을 축(세로 v / 가로 u) 둘레 회전 → 평면 basis 정렬. 닫힌 폴리라인 보장.
- 회전 다이얼로그(각도 1~360, 프리셋 90/180/360 + 축). store `revolveSketchAs`/`openRevolve`. 도구→회전 배선.
- 증명: 폰/꽃병 실루엣 330° 회전 → 깔끔한 축대칭 솔리드 (`revolve-proof.png`).
- 검증 tsc 0 · vitest **63/63** · build OK. PR #14 푸시. SPEC-MAP 4.2 Revolve ✅.

### 4라운드 (기영님: "다음") — 면 위에 스케치(Sketch on Face)
- `planeFromFace(verts, normal)`: 면 정점 중심·법선 → 직교 basis 평면. `SketchPlaneDef.id` string 일반화.
- store `sketchPlane`/`SketchEntity.plane`: PlaneId → **SketchPlaneDef**. `beginSketch` 가 선택된 면 감지→그 면 평면 진입(없으면 기준면 picker).
- SketchLayer 그리기면을 basis 행렬로 정렬(표준+면위 공통). SavedSketches/extrude/revolve 가 def 직접 사용.
- 증명: 박스 윗면·옆면에 원 스케치→돌출 → 각 면 수직 보스/손잡이 (`face-sketch-proof.png`). "쌓아가는 모델링" 성립.
- 검증 tsc 0 · vitest **66/66** · build OK. PR #14. SPEC-MAP 3.1 ✅.

### 5라운드 (기영님: "다음 진행") — 돌출 드래그 핸들 (손맛)
- store `extrudeDrag` 세션 + start/setHeight/commit/cancel. 드래그 중 `gizmoDragging`으로 카메라 차단.
- `ExtrudeDragLayer`: 스케치 1개 선택 시 법선 방향 핸들(구+라인). 끌면 화면 픽셀→높이, 미리보기 솔리드 실시간, 놓으면 새 바디 확정. 높이 mm 라벨.
- 정밀/빼기·합치기는 도구→돌출 다이얼로그 유지(이중 경로).
- 검증 tsc 0 · vitest **68/68** · build OK. PR #14. SPEC-MAP 4.1 갱신.

### 6라운드 (기영님: "다음진행 후속질문3개다") — 4종 일괄
- **카메라 면 정렬**: `alignToNormal` — 면 위 스케치 진입 시 그 면 정면으로 자동 회전.
- **드래그 격자 스냅**: 돌출 드래그 높이를 0.5mm 단위로(snap.grid 연동).
- **모깎기 1차 — 둥근 박스**: RoundedBoxGeometry 프리미티브. 삽입→둥근 박스.
  - 정직: 임의 모서리 일반 fillet 은 메시에서 위상 재구성이라 깨지기 쉬움 → **B-rep(OCCT) 트랙**으로 명시. 가짜 안 줌.
- **데모 브래킷**: 둥근박스+볼트구멍2+보스+보어, 세션 도구만으로 end-to-end. STL/PNG. 삽입→데모 브래킷.
- 검증 tsc 0 · vitest **72/72** · build OK. PR #14. SPEC-MAP 2.2·4.x 갱신.

### 7라운드 (기영님: "메인 머지하고 새단계") 
- **PR #14 → main 머지 완료** (merge commit, 라운드별 히스토리 보존).
- **새 단계: 패턴(선형·원형)** — `transformMesh`(translate/rotate, 위상보존) + store `patternLinear`/`patternCircular` + PatternDialog. 변형→패턴.
- 증명: 박스 Y축 둘레 8개 링 (`pattern-proof.png`). 검증 tsc 0 · vitest **77/77** · build OK.
- **PR #15**(draft) 생성 — 패턴.

### 8라운드 (기영님: "다음 코딩") — 바디 변형(회전·스케일·미러)
- `transformMesh`: centroidOf/scaleMesh/mirrorMesh(반사 winding·법선 뒤집기).
- store `rotateBody`/`scaleBody`/`mirrorBody`(중심기준) + TransformDialog. 변형 플라이아웃 완성.
- 증명: L자+X미러 양쪽 솔리드 정상(`mirror-proof.png`). 검증 tsc 0 · vitest **80/80** · build OK. PR #15.
- SPEC-MAP 10.1 TransformationMatrix ✅(이동+회전+스케일+미러).

### 9라운드 (기영님: "후속질문 전체 진행")
- **PR #15 → main 머지**(패턴+변형).
- **회전 빼기/합치기**: 돌출 cut/fuse 로직을 `combineTools` 헬퍼로 추출·공유. revolveSketchAs(angle,axis,mode) + RevolveDialog 모드.
- **측정 도구**: 측정 모드 → 모델 위 두 점 클릭 → 점선+거리(mm). ShapeMesh 클릭 분기, MeasureLayer, 좌측 측정 버튼.
- 검증 tsc 0 · vitest **83/83** · build OK. **PR #16**(draft). SPEC-MAP 4.2·측정 갱신.

### 10라운드 (기영님 스크린샷: 측정 잘됨 + "스케치 전체 기능·GUI Shapr3D화·바디위 자르기·확대시 그리드 세분")
- **1차**: 어댑티브 그리드(`adaptiveCellSize` — 거리에 따라 셀 10배 단계, 월드+스케치평면) + 스케치 비주얼 Shapr3D 팔레트(선 연흰파랑·점 파란원·면 파랑, 정점 점 표시).
- 검증 tsc 0 · vitest **84/84** · build OK. PR #17(회전·측정·그리드·비주얼 누적).
- **2차 예정(기영님 핵심)**: 자르기(trim, 선 교차) · 면 분할(바디 위 스케치선으로 면 자르기, 닫힘 아니어도) · 점 드래그 편집.

### 11라운드 (기영님: "자르기는 평면 라인 인식해 스케치 연동 구조") — 스케치 자르기(Trim)
- `kernel/sketchTrim.ts`: `trimAt(strokes, uv)` — 클릭 근처 선분을 다른 모든 선분과의 교차점으로 나눠 클릭 구간만 제거. 열린획→두 조각, 닫힌획→열린획.
- store SketchTool "trim" + `trimSketchAt`. 좌측 자르기 버튼 동작화.
- 검증 tsc 0 · vitest **88/88**(자르기 4종) · build OK. PR #17.

### 12라운드 (기영님: "자르기 Shapr3D식 + 나머지 스케치 도구 모두") — 그리기 도구 호·스플라인·삭제
- `kernel/sketchCurves.ts`: catmullRom(스플라인) + arc3(3점 원호).
- store SketchTool spline/arc/delete. 점 도구 통합(isPointTool) + finalizeStroke(확정 시 곡선화). 호 3점 자동확정. 삭제 nearestStrokeIndex.
- 검증 tsc 0 · vitest **94/94** · build OK. PR #17.
- **남은 스케치 도구(순차)**: 미러·패턴·오프셋(변형류) → 투상·텍스트(무거움) → 점 드래그. 자르기 hover 미리보기도 다듬을 것.

### 13라운드 (기영님: "후속질문 그대로 진행") 
- **PR #17 → main 머지**.
- **스케치 변형**: `sketchTransform2d.ts`(mirror/patternLinear/patternCircular/offset) + store `sketchMirror/sketchPatternLinear/sketchPatternCircular/sketchOffset/sketchProject`(바디 모서리 worldToPlane 투영) + SketchTransformDialog.
- 증명: 스케치 원형패턴(6)→돌출 = 페그 링(`sketch-pattern-proof.png`). 검증 tsc 0 · vitest **102/102** · build OK. **PR #18**(draft).
- 남은 스케치 도구: 이동/회전 · 텍스트.

### 14라운드 (기영님 스크린샷: 자르기 안 사라짐·점 크기·점 드래그)
- **점 드래그 편집**: 점 핸들 클릭→드래그 이동(store start/move/endPointDrag, 선택 점 주황). 드래그 중 카메라 잠금.
- **점 크기 화면 비례**(radius*0.012) — Shapr3D 비율. **자르기/삭제 클릭 반경 줌 비례**(radius*0.06)로 적중 개선.
- 검증 tsc 0 · vitest **103/103** · build OK. PR #18.

### 15라운드 (기영님: Shapr3D "Selecting geometry" 숙지해 수정)
- 문서 403(못 읽음) → 문서화된 Shapr3D 선택 모델 적용.
- **면 더블탭 = 바디 전체 선택** 추가(ShapeMesh onDoubleClick). 단일탭=면/모서리, 추가탭=다중토글, 빈곳=해제는 기존 확인.
- 검증 tsc 0 · vitest 103/103 · build OK. PR #18.

## 내일 첫 작업
- 선분(세그먼트) 드래그 이동 + 스케치 이동/회전 + 텍스트(폰트→곡선).
  중기: 진짜 fillet/구속 솔버는 OCCT B-rep 런타임 검증 후 트랙.

## 기영님 환경 메모 (재발 방지)
- `git pull` 시 `2026-06-15.md untracked overwrite` 충돌 → 로컬에 untracked 동명 파일.
  해결: `git stash -u` 또는 그 파일 백업/삭제 후 pull. (원격에 정본 있음)
